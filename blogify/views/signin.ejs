<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./link') %>
    <title>SignIn</title>
  </head>

  <body>
    <div
      class="card"
      style="
        height: auto;
        width: 22rem;
        margin: 100px auto;
        float: none;
        margin-bottom: 10px;
        padding: 45px 10px;
      "
    >
      <div class="card-body">
        <h3 class="card-title" style="text-align: center">SignIn</h3>
        <form action="/user/signin" method="post">
          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input
              type="email"
              class="form-control"
              id="email"
              aria-describedby="email"
              placeholder="abc@example.com"
              name="email"
            />
          </div>
          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input
              type="password"
              class="form-control"
              id="password"
              name="password"
            />
          </div>
          <button
            type="submit"
            class="btn btn-outline-secondary"
            style="margin: 10px 97px; padding: 5px 28px; border-radius: 20px"
          >
            SignIn
          </button>
        </form>
        <% if (locals.error) { %>
        <div class="alert alert-danger" role="alert" style="padding: 7px 54px; border-radius: 12px;" ><%= locals.error %></div>
        <% } %>
      </div>
    </div>

    <%- include('./script') %>
  </body>
</html>
